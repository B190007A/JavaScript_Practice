//fix decimal number
let lumpSum = 120;
let numProduct = 7;

//Using Math. to round
let AvgPrice = Math.round(lumpSum / numProduct);

//Using toFixed to define
console.log(AvgPrice.toFixed(2));